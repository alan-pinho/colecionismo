import 'package:flutter/material.dart';

class ClLoader extends StatelessWidget {
  const ClLoader({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [CircularProgressIndicator()]);
  }
}
